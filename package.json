{
  "name": "TeamChat",
  "version": "0.1.0",
  "description": "A simple team chat system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "restart": "pm2 startOrRestart ecosystem.config.js",
    "deploy": "pm2 deploy ecosystem.config.js production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "coffeescript": "latest",
    "express": "^4.16.3",
    "mustache": "^2.3.0",
    "nedb": "^1.8.0",
    "socket.io": "^2.1.1"
  },
  "engines": {
    "node": "8.11.1"
  },
  "keywords": [
    "node",
    "research"
  ],
  "license": "MIT",
  "devDependencies": {
    "aws-sdk": "^2.274.1",
    "pm2": "^3.0.0"
  }
}
